[build-system]
requires = ["setuptools>=45"]
build-backend = "setuptools.build_meta"

[project]
name = "guv"
authors = [
  {name = "Sylvain Rousseau", email = "sylvain.rousseau@hds.utc.fr"},
]
readme = "README.md"
description = "Programme d'aide à la gestion d'UV"
dynamic = ["version"]
requires-python = ">=3.8"
license = {text = "MIT"}
scripts = {guv = "guv.runner:main"}
dependencies = [
  "doit",
  "jinja2",
  "jsonschema",
  "latex",
  "markdown",
  "mechanicalsoup",
  "numpy",
  "openpyxl",
  "oyaml",
  "pandas",
  "prompt_toolkit",
  "pynliner",
  "requests",
  "schema",
  "unidecode",
  "xlrd",
  "yapf",
]

[dependency-groups]
dev = [
    "pytest>=8.3.5",
    "pytest-dependency",
    "pytest-order",
    "shtab"
]
doc = [
    "tox",
    "sphinx",
    "furo",
    "schema",
    "myst_parser"
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
guv = ["schemas/*", "data/*", "images/*", "data/templates/*"]

[tool.setuptools.dynamic]
version = {attr = "guv.__version__"}
